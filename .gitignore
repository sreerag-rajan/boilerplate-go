# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Project binaries
# e.g., go build, go get, go install, etc.
bin/

# Project build cache (remove the comment below to include it)
# ./.go-build/

# Project coverage files (remove the comment below to include it)
# ./.go-cover/

# Go module download
# go.mod and go.sum files are generated by go build
# and should be under version control
# However, the go.sum file is generally not checked into revision control
# to avoid accidentally leaking sensitive information
# go.mod
# go.sum

# Project specific ignore
.env

